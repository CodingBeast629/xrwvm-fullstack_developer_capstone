services:
  mongo_db:
    image: mongo:6
    container_name: mongo_db
    ports:
      - "27017:27017"

  api:
    build: .
    container_name: node_api
    ports:
      - "3030:3030"
    depends_on:
      - mongo_db
